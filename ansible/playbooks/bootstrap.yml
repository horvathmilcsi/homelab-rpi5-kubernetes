---
- name: Bootstrap Raspberry Pi for k3s
  hosts: localhost
  become: yes
  roles:
    - common   # OS updates, install Midnight Commander
    - k3s      # Install k3s (!without editing /boot/firmware/cmdline.txt !)
